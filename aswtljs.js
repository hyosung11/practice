// var animals = ["goat", "cat", "crow"];
// var products = ["milk", "cheese", "burger"];
// var foodItems = [];
// var k = 0;
//
// for (var i = 0; i < animals.length; i++) {
//   for (var j = 0; j < products.length; j++) {
//     foodItems[k] = animals[i] + products[j];
//     k++;
//   }
// }
//
// console.log(foodItems);
//
//   for (var n = 0; n < 10; n++) {
//     alert("This is better")
//   }
//
//   for (var n = 0; n < array.length; n++) {
//     num = 11;
//   }
//
//   for (var i = 0; i < 5; i++) {
//     for (var j = 0; j < 5; j++) {
//     }
//   }
//
//   for (var i = 0; i < 5; i++) {
//     for (var j = 0; j < 5; j++) {
//     }
//   }
//
// for (var i = 0; i < 5; i++) {
//   for (var j = 0; j < 5; j++) {
//     total = i + j;
//   }
// }
//
// for (var i = 0; i < animals.length; i++) {
//   for (var j = 0; j < products.length; j++) {
//     foodItems[k] = animals[i] + products[j];
//     k++;
//   }
// }
//
// for (var i = 0; i < 2; i++) {
//   for (var j = 0; j < 4; j++) {
//     alert(i + j);
//   }
// }

// var array1 = [1, 2];
// var array2 = ["monkeys", "pigs"];
// var array3 = [];
// var k = 0;
//
// for (var i = 0; i < array1.length; i++) {
//   for (var j = 0; j < array2.length; j++) {
//     array3[k] = array1[i] + array2[j];
//     k++;
//     alert(array1[0] + array2[0])
//   }
// }

// var colors = ["blue", "gold"];
// var teams = ["Michigan", "Barca"];
// var favorites = [];
// var n = 0;
// for (var i = 0; i < colors.length; i++) {
//   for (var j = 0; j < teams.length; i++) {
//     favorites[n] = colors[i] + teams[j]
//     n++;
//   }
// }
// alert(favorites[0]);
//
// function calcDiscount(amt, percentage) {
//   return amt - (amt * percentage);
// }
//  // code the statement that passes the value of total back to the calling code.
//  return total;
//
//  var total = calc(addIt(merch, services));
//
// // Code a function call that has two arguments. The first argument is a variable. The second argument is another function call. The second function call takes the argument 10. The value returned by the first function is assigned to est, which hasn't been declared beforehand.
// var est = firstFunction(xyz, secondFunction(10));

// Code a function with two parameters that passes back the values of the parameters multiplied by each other. Use minimal code. That is, don't assign the product of the multiplication to a variable.
// function product(num1, num2) {
//   return num1 * num2;
// }
// console.log(product(11, 12));

// const product = (num1, num2) => {
//   return num1 * num2;
// }
// console.log(product(11, 12));

// Code a statement that assigns to a variable (that hasn't been declared beforehand) the arithmetic sum of two function calls. The function calls take no arguments.
var sum = functionOne() + functionTwo();

// Code an alert. Specify as the message a function call that takes two literal strings as arguments.
alert(myFunction("literal string one", "literal string two"));

// Code a function that has three parameters to which numbers are passed. Add them and assign the total to a variable that hasn't been declared beforehand. If the number is greater than 0, return it to the calling code.
// function together(num1, num2, num3) {
//   var total = num1 + num2 + num3;
//   if (total > 0) {
//     return total;
//   }
// }

// Complete the statement with a call to calc that takes the function addIt as an argument. addIt takes merch and services as arguments.
// var total = calc(addIt(merch, services));

// Code a function call that has two arguments. The first argument is a variable. The second argument is another function call. The second function call takes the argument 10. The value returned by the first function is assigned to est, which hasn't been declared beforehand.
// var est = thisFunction(num, anotherFunction(10));
//
// function total(num1, num2) {
//   return num1 * num2;
// }
//
// var total = partOne() + partTwo

// function computeInterest(principal, percentage) {
//   return principal * percentage;
// }
// alert(computeInterest(100, .10));

// I've coded a function call that concatenates two text fragments to create a message for the alert. Code the function. Leave my code as-is. Add your code below it.
function concatFrags(frag1, frag2) {
  return frag1 + frag2;
}
